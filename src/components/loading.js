
export default function Loading(params) {
    return(
        <div className="absolute inset-0 flex justify-center items-center w-full h-full bg-black bg-opacity-60">
            <h1 className="text-white text-xl text-white font-Quicksand font-semibold">Loading...</h1>
        </div>
    )
}